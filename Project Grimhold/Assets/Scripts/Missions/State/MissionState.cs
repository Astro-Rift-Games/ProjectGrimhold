/// <summary>
/// The current lifecycle state of a mission instance.
/// </summary>
public enum MissionState
{
    Disponible,
    Activa,
    Completada,
    PendienteDeReclamar,
    Reclamada,
    Abandonada
}
