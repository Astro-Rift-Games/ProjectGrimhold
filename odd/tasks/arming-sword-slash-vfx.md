# ArmingSword Slash VFX

Objective: Implement a local, confirmed-attack ArmingSword Slash vertical slice on Animation-VFX without changing gameplay, networking, or shared directional attack animation sets.

Baseline: HEAD 9b248ca915ef12b79b73981dff3519c7a303df85; untracked `Project Grimhold/Assets/Art/VFX.meta`, `Project Grimhold/Assets/Art/VFX/VFX-Slash.png` and `.meta` are user-provided and must be preserved. Golden reference `C:/Users/Dani/OneDrive/Escritorio/VFX-Slash.gif` establishes only four-frame order, 100 ms cadence, and relative frame alignment; it does not establish six-direction pose, orientation, sorting, or swing timing. Existing PNG is 384x96 (four 96x96 cells); current .meta tight-crops sprites with individual centered pivots. Source and architecture audit in prior conversation maps `AttackPerformedEvent` confirmed weapon catalog index/direction, `Sword1H` shared animation set, hand grip, weapon pinning, and local audio. Unity 6000.5.1f1 executable is available; Unity MCP unavailable. Source and user-supplied GIF are the authority, not historical notes.

Scope: Fix import alignment first. Then use Unity to determine directional pose/orientation/sorting/timing against actual ArmingSword swing before runtime implementation. Add minimal optional ArmingSword presentation config and local sprite one-shot on confirmed attacks, tests and prefab configuration. Preserve switching, proxies, late-join and Host Migration behavior; no new network state. Exclude impact VFX, other archetypes, general VFX framework/pooling and gameplay modifications. Do not modify unrelated assets. No commit or push until separately authorized (prior explicit no-commit/no-push instruction).

TDD: user explicitly disabled strict TDD for T1 and explicitly ENABLED strict TDD for T3. T3 requires observed RED on focused Unity EditMode tests before runtime implementation, GREEN, then refactor, with evidence; functional verification remains required. Exact Unity runner is Unity 6000.5.1f1 batchmode EditMode without `-quit`, outputs in system TEMP, plus Unity-mediated visual observation and runtime multiplayer checks where feasible.

Route and workload: T1 multi-file asset/test if necessary delegated to gentle-ai-worker; T2 Unity-based inspection delegated to a bounded read-only worker; T3 multi-file implementation delegated to one gentle-ai-worker, but only after T2 findings; T4 delegated verification. Forecast ~250–450 authored lines excluding asset YAML. Delivery strategy ask-on-risk; no commit/PR authorized for this feature yet.

## Tasks
- [x] T1 Preserve the four frame compositions during Unity import. Four full-cell 96x96 rects and shared centered pivots configured in VFX-Slash.png.meta while preserving sprite IDs/GUID and PNG; focused SlashSpriteImportTests.cs added. Evidence: Python YAML check passed; Unity 6000.5.1f1 on isolated TEMP project after correcting test-only Z-depth assumption yielded `SlashFrames_ImportInCellOrderWithSharedCenteredPivot` 1 passed / 0 failed in `%TEMP%/arming-slash-import-isolated-v2.xml` and exit 0. Imported sprites have expected rect/pivot and XY bounds. GIF validates order/cadence/relative positions, not directional pose or timing. No original tracked drift.
- [x] T2 Inspect real ArmingSword swings through Unity. Evidence: TEMP-only Unity 6000.5.1f1 Editor diagnostic instantiated actual NetworkPlayer prefab in normal temporary scene, sampled six actual Sword1H directional clips over eight times (0–0.55 s) with directional Idle body and actual ArmingSword sprite/grip/angle; rendered 48 base and 30 overlay PNGs under `%TEMP%/grimhold-slash-pose-capture/` and measured positions in poses.csv. Actual 0.6 s swing rotates sword from lateral at 0.1–0.2 to vertical at 0.3–0.4. A slash parented to moving hand double-moved and detached in final frame; fixed VisualRoot anchor (0.2, -0.3) in S/SE/SW and (0.2, 0) in N/NE/NW, 0.65 uniform visual scale, vertical mirror for N/NE/NW, no rotation, and four 100 ms frames over attack clip 0.1–0.5 gave the best observed alignment. Sword sorting 20 front / -10 back; effect +1 relative to sword, layer Characters (hand 30 front / -2 back in diagnostic). North final frame can be occluded behind body as it fades. This is an Editor visual fixture approximating PlayerWeaponPresenter placement without Fusion, NOT Play Mode or multiplayer validation; runtime tuning remains T4.
- [x] T3 Implement minimal ArmingSword-only Slash presentation/configuration and prefab hookup from T2 measurements. `WeaponDefinition.Presentation` owns optional four frames; ArmingSword alone assigns them. `PlayerAttackSlashPresenter` subscribes to confirmed `AttackPerformed`, resolves catalog identity once, rejects ranged/other/invalid/dead attacks, waits for matching `RightHand` clip, shows frames at 0.1–0.5s, clears on completion/interruption/defeat/disable; renderer is under VisualRoot and no new network state. Evidence: strict TDD RED `%TEMP%/arming-slash-t3-red.xml` missing frames, GREEN `%TEMP%/arming-slash-t3-green.xml` 3/3; additional defeat RED 2/3 and GREEN 3/3, triangulation final 11/11 in `%TEMP%/arming-slash-t3-triangulation.xml`. Note: isolated EditMode reflection verifies captured frames and six poses, not a live Equipment switch. Production prefab/renderer sampled in Unity TEMP fixture, not Fusion runtime. Cadence: TEMP probe drove actual Animator+presenter for six facings and recorded four ordered frames. Initial EditMode test incorrectly counted time before entering Attack; corrected test first entered Attack then asserted clip phase 0.30 and 0.50. Differential TEMP run WITHOUT tolerance gave RED 11/12 (`VFX-Slash_1` at actual 0.30 clip phase), then +0.0001s production tolerance gave GREEN 12/12 and correct completion at 0.50. Evidence: `%TEMP%/arming-slash-cadence-no-tolerance.xml`, `%TEMP%/arming-slash-full-final-cadence.xml`. No claim of Fusion runtime.
- [ ] T4 Verify import, targeted EditMode tests, Unity compilation, visual six-direction behavior and available proxy/switch paths; review diff and unrelated changes. Status: in progress. Focused import 1/1, slash 12/12 and animator 38/38 passed in isolated Unity; full final EditMode 1464 total/1431 passed/33 failed, EXACT same 33 failing fullnames as prefeature 1451/1418/33. Focused PlayerCombat PlayMode 0/3 in candidate AND pristine HEAD baseline, identical three failure names/messages (no candidate-specific regression). TEMP visual fixture confirmed production prefab Slash renderer/layer in six captures with manually applied poses; actual presenter runtime, multiplayer proxies, weapon switching and Host Migration remain unobserved, not claimable as passed. Structural review done: scoped tracked diff covers NetworkPlayer.prefab, ArmingSwordWeaponDefinition.asset, WeaponDefinition.cs and PlayerCombatArchitecture.md; untracked source/meta/tests/VFX/odd all enumerated; no scene/package/font changes remain, `git diff --check` exit 0 (prefab/doc CRLF warnings only), new asset GUIDs unique. ~600 authored lines including tests and prefab; review workload should be sliced before a PR if requested. T4 is NOT complete because live Host+Client/Proxy Play Mode, attack→switch, failed attack, six facings, sorting, defeat/interruption and Host Migration continuity are not observed. Do not reopen original Unity via automation because it previously rewrote two font assets; user-controlled Editor runtime observation is the next gate.

Incident: after the interactive Unity Editor closed, two previously clean tracked font assets were content-truncated (alagard SDF 2.14 MB→7.7 KB, LiberationSans fallback 535 KB→9.7 KB; not line-ending-only). User explicitly authorized restoring only those two files from HEAD; `git restore --source=HEAD --worktree -- <those two paths>` completed and status shows no tracked drift. Do not rerun Unity on original project while this risk persists. Use an isolated temporary project COPY (not a Git worktree) for Unity verification and visual inspection, keeping source tree untouched. T1 import test passed in isolated Unity copy; no runtime before T2 visual evidence.

Next: user-controlled live Unity Host/Client observation: (1) six ArmingSword swings and VFX at 0.1–0.5s/front-back, (2) confirm and immediately switch to Rapier then back, (3) invalid/blocked attacks and another weapon show no Slash, (4) proxy and late-join, defeat/interruption and migration restore. Watch for reimport of the two font assets before accepting scene changes. If observations fail, reopen T3 with reproducible evidence. No commit/push without new authorization.
