# Shared Attack VFX clip

Objective: Replace the committed ArmingSword-only Slash playback with the first consumer of a shareable, sprite-clip-based Attack VFX configuration. Approved by user: `WeaponDefinition.Presentation` references a reusable `AttackVfxDefinition` asset analogous to directional attack sets. The asset owns one sprite-only `AnimationClip`, start timing and six directional poses. No second Animator, particles, trails, speculative playback framework or network state. Future Thrust/Cast can be configured without changing the presenter when using the same sprite-clip, directional attack-clip contract.

Baseline: `Animation-VFX`, clean HEAD `de9aa80 Slash VFX` on entry. Previous slice evidence in `odd/tasks/arming-sword-slash-vfx.md`; preserve all existing import assets, sprite IDs, scene/prefab GUIDs, confirmed weapon identity and six facings. Prior explicit no commit/push instruction remains in force. Unity must run only on `%TEMP%/grimhold-slash-unity-isolated` because original Editor previously damaged unrelated font assets. No Unity MCP available. Architecture: `Project Grimhold/Docs/Architecture/PlayerCombatArchitecture.md` and `GameFeelParticlesArchitecture.md`; original slice T4 Fusion runtime checks still outstanding.

Scope and contracts: migrate ArmingSword's four sprite references into an AnimationClip animating only the dedicated `SpriteRenderer.sprite`. Put start offset and one pose per visual direction (position, scale, rotation, sorting) in a reusable `AttackVfxDefinition` ScriptableObject; `WeaponDefinition.Presentation` holds an optional reference. Generic local presenter samples the clip using matching RightHand attack phase, never wall-clock animation or frame-index arithmetic; no hardcoded Slash or melee weapon behavior. Preserve event snapshot, interruption, defeat, disable, duration and proxy semantics. Validate serialized clip bindings, config and prefab references in isolated Editor tests. Clip is a presentation asset, not combat authority. Keep current sprite import, shared Sword1H attack clips and gameplay untouched.

TDD: prior explicit strict TDD instruction was T3 of the earlier slice, not a new project-wide setting. For this refactor use observed test-first focused RED/GREEN where feasible and record exact tests; never infer that the whole project has strict mode enabled. Runner: Unity 6000.5.1f1 `-batchmode -projectPath <isolated TEMP> -runTests -testPlatform EditMode -testFilter <focused suite> -testResults <TEMP XML> -logFile <TEMP log>` without `-quit`. Verify exact candidate copy is synchronized before each Unity run.

Forecast: approximately 350–550 authored diff lines across reusable asset/config, clip serialization, presenter/prefab migration, tests and architecture; avoid gratuitous additions and protect review workload. Delivery strategy: ask-on-risk; no commit or push without explicit new authorization. Route: multi-file writer (`gentle-ai-worker`) for implementation, `gentle-ai-verify` for command execution/independent validation as indicated by read-only ASSESS (RDD off); parent owns scope, history and final report.

## Tasks
- [x] T1 Implement shared sprite-clip Attack VFX configuration, ArmingSword migration and generic presenter with focused checks. Expected files: `WeaponDefinition.cs`, new `AttackVfxDefinition.cs/.meta`, new Attack VFX `.asset/.meta`, new sprite-only `.anim/.meta`, renamed presenter `.cs/.meta` preserving GUID, `NetworkPlayer.prefab`, focused EditMode tests, PlayerCombatArchitecture.md. Route delegated multi-file writer; no unrelated changes. Validate clip binding, sprite order, start/end and six poses in Editor; retain current confirmed-attack behavior.
- [x] T2 Verify complete diff, focused and full isolated Unity EditMode regression, compare baseline failing names, prefab/asset serialized references and unintended drift. Focused `%TEMP%/shared-attack-vfx-implemented-v2.xml` **11/11**; full `%TEMP%/shared-attack-vfx-full.xml` **1463 total / 1430 passed / 33 failed**, exact same 33 failing test fullnames as prior slice 1464/1431/33 and prefeature baseline 1451/1418/33; `SlashSpriteImportTests` 1/1 passed. Unity compilation success; exit 2 comes from the 33 pre-existing test failures. `git diff --check` exit 0 with LF→CRLF warnings only. All added GUIDs unique; presenter GUID retained; no font, scene, controller, package, or generated tracked drift after these Unity runs. Read-only native ASSESS reported `unassessable` because untracked files need declaration, so the independent `gentle-ai-verify` full run was performed. No strict RED observed for this refactor, and no commit/push per user instruction.
- [ ] T3 Observe real Fusion Host+Client/proxy attack, immediate weapon switch, interruption/defeat, six-facing visual quality/sorting, late-join and Host Migration after this refactor. No runtime verification is claimed from isolated EditMode/fixture results; original project Unity automation remains unsafe due recurrent font truncation.

Resolved investigation (Unity 6000.5.1f1, isolated TEMP only): initial implementation compiled after fixing CS0177 and removing an obsolete TEMP-only diagnostic that still referenced the renamed presenter. Focused suite ran 14 cases: 13 passed, 1 failed (`ArmingSwordSouthAttack_SamplesVfxClipAtAttackPhase`); the renderer remained disabled with sprite null already at attack clip phase 0.15. Direct focused `AnimationClip.SampleAnimation` on dedicated SpriteRenderer with a valid sprite-only clip and four keys ran 1/1 failed (sprite null), as did a TEMP-only legacy clip differential and a legacy `Animation` component, even when the object-reference curve was regenerated in memory with `AnimationUtility.SetObjectReferenceCurve`. Evidence: `%TEMP%/shared-attack-vfx-focused-v3.xml`, `shared-attack-vfx-diagnostic.xml`, `shared-attack-vfx-sample-direct.xml`, `shared-attack-vfx-sample-legacy.xml`, `shared-attack-vfx-legacy-component.xml`, `shared-attack-vfx-generated-clip.xml`. Direct child-root sampling was not a supported playback premise for that discarded candidate. It had no GREEN tests; the subsequent existing-Animator-root implementation and its verification are recorded below.

Follow-up authorized isolated feasibility audit (no production edits): an Editor-only proof instantiated the real `NetworkPlayer` prefab and generated in memory a sprite-only clip binding to the generic direct child path `AttackVfx` relative to its *existing* `VisualRoot` Animator. `clip.SampleAnimation(animator.gameObject, phase)` assigned the child renderer's sprite, unlike sampling the child directly. A second test drove the real RightHand attack state and sampled the VFX at `RightHand` clip phase minus the configured 0.1s start (with 0.1ms boundary tolerance); frames at 0.15/0.30/0.40s matched, and sampling did not change RightHand normalized time or hand local position. **2/2 passed** in `%TEMP%/existing-animator-vfx-phase.xml`; no Character.controller edits, state/layer additions, second Animator, per-weapon routing or new clock. Clip was generated in memory; serialized asset import, cleanup/interruptions, full regression, proxy/Fusion runtime and visual quality remain unproven. The TEMP project contains abandoned candidate files and proof fixtures, not production changes.

Safety: rolled back only the failed attempt's source/asset/prefab/test/docs changes to HEAD `de9aa80`, preserving the prior committed Slash slice; tracked diff was empty immediately afterward. No commit/push. **New incident after the TEMP-only proof:** two original tracked TMP font assets became content-truncated again (`Assets/Art/Fonts/alagard/alagard SDF.asset`: HEAD 2,139,882 bytes → 7,687; `Assets/TextMesh Pro/Resources/Fonts & Materials/LiberationSans SDF - Fallback.asset`: HEAD 534,540 → 9,748; git diff 9/1837 and 9/36). At entry the original repo was clean; both original files have the same new local mtime. The TEMP copies are separate files, not hardlinks; no Unity.exe process remained at diagnosis. Attribution to the isolated command or external Editor/sync is unproven. User separately authorized restoring exactly the two truncated fonts from HEAD; `git restore --source=HEAD --worktree -- <those two paths>` succeeded and normalized content matches HEAD for both. `git status` now shows only this untracked task document; no other tracked drift. User explicitly authorized implementation after the isolated existing-Animator-root feasibility report. T1 implementation: `WeaponDefinition.Presentation` now references an optional `AttackVfxDefinition` asset holding Unity-authored sprite-only `SwordSlashVfx.anim`, 0.1s start and six serialized poses. The renamed `PlayerAttackVfxPresenter` keeps its component GUID, resolves confirmed snapshot weapon+direction, samples `clip.SampleAnimation(_animator.gameObject, rightHandPhase - start)` on existing `VisualRoot` Animator, and clears on interruption/defeat/disable/completion. Prefab child is generic `AttackVfx`; no controller changes, new states/layers or extra Animator. The Unity-authored clip binds only `AttackVfx/SpriteRenderer.m_Sprite` and has four keys 0/.1/.2/.3, duration .4. Initial isolated compile blocked on CS0269 (out error definite assignment); fixed and resynced. Focused isolated Unity EditMode suite `%TEMP%/shared-attack-vfx-implemented-v2.xml`: **11/11 passed**, compiler warnings only; original fonts remained clean after the run. Strict RED was not observed for this refactor; do not claim strict TDD evidence. No commit/push per earlier user instruction. Next: user-controlled Fusion Host+Client visual/runtime checks remain T3; preserve original fonts and inspect status if opening Unity. If fonts drift again, stop and request new authorization rather than treating this approval as blanket permission. No commit/push without new authorization. Do not silently reintroduce presenter-side manual Sprite[] stepping.
